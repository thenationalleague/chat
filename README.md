# chat

`chat` is a placeholder repository for a future chat-related project. The application architecture, language, framework, and runtime have not been chosen yet.

## Current status

This repository currently contains project documentation only. Source code, dependency manifests, build scripts, tests, and deployment configuration have not been added yet.

## Setup

No dependency installation steps are available yet.

Once a technology stack is selected, document the setup command here, for example:

```sh
# Example only; replace with the actual project setup command.
npm install
```

## Development commands

No build, lint, format, or test commands are available yet.

When these commands are introduced, document them here:

```sh
# Build
TBD

# Lint
TBD

# Format
TBD

# Test
TBD
```

## Recommended next steps

1. Choose the project technology stack.
2. Add the initial source tree and dependency manifest.
3. Add build, lint, format, and test commands.
4. Update this README and `AGENTS.md` with the final commands.
